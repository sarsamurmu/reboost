import { ReboostPlugin } from 'reboost';

declare namespace {{ name }}Plugin {
  export interface Options {

  }
}

function {{ name }}Plugin(options: {{ name }}Plugin.Options = {}): ReboostPlugin {
  return {
    name: '{{ lowerCase name }}-plugin'
  }
}

export = {{ name }}Plugin;
