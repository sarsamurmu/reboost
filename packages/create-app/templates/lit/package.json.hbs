{
  "name": "{{ lowerCase (dashCase appName) }}",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "dev": "node reboost"
  },
  "license": "MIT",
  {{#with versions}}
  "devDependencies": {
    "reboost": "{{ reboost }}"
  },
  "dependencies": {
    "lit": "{{ [lit] }}"
  }
  {{/with}}
}
