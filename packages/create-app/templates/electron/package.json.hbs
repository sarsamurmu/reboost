{
  "name": "{{ lowerCase (dashCase appName) }}",
  "version": "0.0.1",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "dev": "node reboost",
    "start": "electron ."
  },
  "license": "MIT",
  {{#with versions}}
  "devDependencies": {
    "reboost": "{{ reboost }}"
  },
  "dependencies": {
    "electron": "{{ electron }}"
  }
  {{/with}}
}
