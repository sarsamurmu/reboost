{
  "name": "{{ lowerCase (dashCase appName) }}",
  "version": "0.0.1",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "dev": "node reboost",
    "start": "electron ."
  },
  "license": "MIT",
  "devDependencies": {
    "reboost": "{{ versions.reboost }}"
  },
  "dependencies": {
    "electron": "{{ versions.electron }}"
  }
}
