{
  "name": "{{ lowerCase (dashCase appName) }}",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "start": "node reboost"
  },
  "license": "MIT",
  "dependencies": {
    "lit-element": "{{ versions.litElement }}"
  },
  "devDependencies": {
    "reboost": "{{ versions.reboost }}"
  }
}
